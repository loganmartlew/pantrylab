{
  "$schema": "http://json-schema.org/schema",
  "$id": "ApiResource",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Resource name (plural)",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "project": {
      "type": "string",
      "description": "The nestjs project where the component will be located.",
      "x-prompt": "Which nestjs project will this component be located in?",
      "x-dropdown": "projects"
    }
  },
  "required": ["name", "project"]
}
