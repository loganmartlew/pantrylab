import { RequestValidationErrorFilter } from './request-validation-error.filter';

describe('RequestValidationFilterFilter', () => {
  it('should be defined', () => {
    expect(new RequestValidationErrorFilter()).toBeDefined();
  });
});
