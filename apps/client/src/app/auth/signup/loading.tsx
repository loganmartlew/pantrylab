'use client';

import { FC } from 'react';
import LoadingScreen from '../../../components/LoadingScreen';

const SignupLoading: FC = () => {
  return <LoadingScreen />;
};

export default SignupLoading;
